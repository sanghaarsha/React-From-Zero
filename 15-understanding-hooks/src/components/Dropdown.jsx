import React from "react";

const Dropdown = (props) => {
    return (
        <React.Fragment>
            <h1>Hello from dropdown!</h1>
        </React.Fragment>
    );
};

export default Dropdown;
